@*@{Layout = "_LyIndex";}*@
@using MyErp.Models
@model TCTrSched
@using MyErp.Resources
@inject LocService SharedLocalizer
@functions {
    int? Planta(){
        int? retcol = (int?)ViewData["Plant"];
        return retcol;
    }
    int Truck(){
        int  retu = 0;
        try{retu =(int)ViewData["truck"];}catch{}
        return retu;
    }
}
<div class="cont-titles">
<h3>@SharedLocalizer.GetLocalizedHtmlString("TruckScheduleCreate")</h3>
</div>
@using (Html.BeginForm()) {
   <div class="div-edit">
    @Html.ValidationSummary(true)
    <table class="table-edit" >
        <input type="hidden" id = "TrScTruck" name = "TrScTruck" value ="@Truck()">
          <tr>
            <td><input type="submit" class="dropbtn" value="Add"  name="actionType"/></td>
            <td><input type="submit" class="dropbtn" value="Cancel" name="actionType"/></td>
        @Html.Partial("_pshowSTrucks",Model)
        </tr>
    </table>

   </div>
}