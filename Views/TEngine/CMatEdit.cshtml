@{Layout = "_LyIndex";}
@using MyErp.Models
@model VTMComponent
@functions {
}
<div class="cont-titles">
<h1>Edit a Material Component record</h1>
</div>
@using (Html.BeginForm(new{Mid = Model.CoRefId})) {
   <div class="visible">
    @Html.ValidationSummary(true)
    <table class="table-norm" border ="1">
        @Html.Partial("_pshowCMaterials",Model)
         <tr>
            <td><input type="submit" value="Update"  name="actionType"/></td>
            <td><input type="submit" value="Cancel" name="actionType"/></td>
        </tr>
    </table>
    </div>
}