@{Layout = "_LyIndex";}
@using MyErp.Models
@model VTMLocation
@functions {
}
<div class="cont-titles">
<h1>Add a New Material Location record</h1>
</div>
@using (Html.BeginForm(new{id = Model.LoRefId})) {
   <div class="visible">
    @Html.ValidationSummary(true)
    <table class="table-norm" border ="1">
        @Html.Partial("_pshowLMaterials",Model)
         <tr>
            <td><input type="submit" value="Add"  name="actionType"/></td>
            <td><input type="submit" value="Cancel" name="actionType"/></td>
        </tr>
    </table>
    </div>
}