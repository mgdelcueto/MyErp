@{Layout = "_LyIndex";}
@using MyErp.Models
@model TFacility
@using MyErp.Resources
@inject LocService SharedLocalizer

@functions {
    int? Planta(){
        int? retcol = (int?)ViewData["Plant"];
        return retcol;
    }
}
<div class="cont-titles">
<h1>@SharedLocalizer.GetLocalizedHtmlString("FCrea")</h1>
</div>
@using (Html.BeginForm()) {
   <div class="visible">
    @Html.ValidationSummary(true)
    <table class="table-norm" border ="1">
        @Html.Partial("_pshowFacilities",Model)
          <tr>
            <td><input type="submit" value="Add"  name="actionType"/></td>
            <td><input type="submit" value="Cancel" name="actionType"/></td>
        </tr>
    </table>

   </div>
}