@*
@using Microsoft.AspNetCore.Builder
@using Microsoft.AspNetCore.Localization
@using Microsoft.AspNetCore.Mvc.Localization
@using Microsoft.Extensions.Options

@inject IViewLocalizer Localizer
@inject IOptions<RequestLocalizationOptions> LocOptions
@using MyErp.Resources
@inject LocService SharedLocalizer
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
    var cultureItems = LocOptions.Value.SupportedUICultures
        .Select(c => new SelectListItem { Value = c.Name, Text = c.DisplayName })
        .ToList();
}
*@

@using MyErp.Resources
 
@inject LocService SharedLocalizer

<div id="page-wrapper">
    <div id="page" class="container-fluid mt-0">
        <div id="page-content" class="row">
            <div id="region-main-box" class="col-12">
                <section id="region-main" class="col-12" aria-label="Content">
<div class="row justify-content-center">
<div class="col-xl-6 col-sm-8 ">
<div class="card">
    <div class="card-block">
            <h1 class="h2 card-header text-center" aria-label="Julia High School: Log in">@SharedLocalizer.GetLocalizedHtmlString("About")</h1>
        <div class="card-body">
            <div class="row justify-content-md-center">
                    <form class="mt-3" method="post" id="privacy" role="form" asp-controller="Privacy" asp-action="Privacy" asp-route-returnUrl="@Context.Request.Path">
                        <div class="form-group">
                            <label for="username" class="sr-only">myERP is a open source project that </label>
                            <label for="username" class="sr-only">tries to create an MRP system aimed  </label>
                            <label for="username" class="sr-only">at small and medium-sized companies</label>
                            <label for="username" class="sr-only">Vigo winter 2021</label>
                            <label for="username" class="sr-only">contact phone: +34 616454045 </label>
                            <a href="mailto:mgdelcueto@gmail.com">contact mail</a>
                            <a href="http:axdebian22">Visit training course myERP</a>
                        </div>
                        <div class="form-group">
                        </div>
                            <div class="rememberpass mt-3">
                            </div>
                        <button name ="actionType" value="Continue" type="submit" class="btn btn-secondary btn-block" >@SharedLocalizer.GetLocalizedHtmlString("Continue")</button>
                    </form>
                                    <div class="col-md-5">
                    <div class="forgetpass mt-3">
                    </div>

                    <div class="form-group">
                    </div>
                </div>


            </div>
        </div>
    </div>
</div>
</div>
</div></div>
                    
                </section>
            </div>
        </div>
    </div>

