@using MyErp.Models
@model TPContract
@using MyErp.Resources
@inject LocService SharedLocalizer
@{
    Layout = "_LayPer";
}

<div class="cont-titles">
<h3>@SharedLocalizer.GetLocalizedHtmlString("ContractCreate")</h3>
</div>
@using (Html.BeginForm()) {
    <div class="visible">
    @Html.ValidationSummary(true)
    @*
    <table class="table-norm" border="1">
        <tr>
            <input type="hidden" name="ConPerId" value="@(((TPerson)ViewBag.Person).PerId)"/>
            <td>@SharedLocalizer.GetLocalizedHtmlString("Name")</td>
            <td>@(((TPerson)ViewBag.Person).PerName)</td>
        </tr>
        <tr>
            <td>@SharedLocalizer.GetLocalizedHtmlString("Surnames")</td>
            <td>@(((TPerson)ViewBag.Person).PerName1)</td>
        </tr>
    </table>
    *@
    <table class="table-norm" border="1">
        <hr style="width:100%;text-align:left;margin-left:0">
        <tr>
            <td>@Html.LabelFor(p => p.ConRevDate,new { @class = "norm-text",})</td>
            <td>@Html.EditorFor(p => p.ConRevDate, new { htmlAttributes = new { @class = "norm-text"} })</td>
        </tr>
        <tr>
            <td></td>
            <td>@Html.ValidationMessageFor(p => p.ConRevDate)</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(p => p.ConObs,new { @class = "norm-text",})</td>
            <td>@Html.EditorFor(p => p.ConObs, new { htmlAttributes = new { @class = "norm-text", @style = "width:455px"} })</td>
        </tr>
        <tr>
            <td></td>
            <td>@Html.ValidationMessageFor(p => p.ConObs)</td>
            <input type="hidden" name="ConPerId" value="@(((TPerson)ViewBag.Person).PerId)"/>
        </tr>
        <tr>
            <td></td>
            <td><input type="submit" value="Add"  name="actionType"/></td>
            <td><input type="submit" value="Cancel" name="actionType"/></td>
        </tr>
    </table>
    </div>
}